const carro = ['ford', 'mazda', 'honda', 'toyota'];

let i = 0;

/* while (i < carro.length) { // el ciclo se seguirá ejecutando siempre y cunado la condición sea verdadera
    console.log(carro[i]);
    i++; // aumentamos el iterador 
} */

while( carro[i]){ // esta forma de escribirlo es similar a la de arriba y cumple la misma función
    console.log(carro[i]);
    i++
}
